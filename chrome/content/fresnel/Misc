Pseudocode for Fresnel display algorithm

input: a single resource ($resource), e.g., a foaf: foaf.rdf#me

	$type = type of $resource
	$instance = instance of $resource //specific URI
	$lenses[] = all lenses with domains matching type or instance //NO REASONER!!!
	
----------------------------------
Notes:
----------------------------------
SELECT ?z1
WHERE
{ <http://foaf.rdf#hmslydia> <http://...type> ?z1}

If the set of applicable lenses contains both lenses whose domain is a classLensDomain and lenses whose domain is an instanceLensDomain, remove lenses whose domain is a classLensDomain.

$lens = if only one default lens choose it
		else choose a default lens 	//possibly algorithm or reasoning.
		else non-default lens